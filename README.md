随便写的简陋webdav，方便我整理资源。

推荐配合电脑端的dufs使用。

主要功能就一个，预览图片时，可以通过点按屏幕左右来实现切换，并有一个不需要确认的删除按钮（可以配置按钮的位置），这样我精选起来快一些。

现在加了回收站，删除的文件会到该盘根目录.webdav_trash文件夹中，支持还原功能，把文件还原到原文件夹中，并自动处理文件重名等。

回收站和设置都在右上角，要点进对应的盘才有对应盘的回收站（都是为了同盘移动快，追求效率）。

另外设置会自动保存填过的IP和端口。
